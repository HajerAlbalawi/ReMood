# ReMood

## رفيقك الشخصي لاختيار الكتاب المناسب لمزاجك

ReMood هو تطبيق متعدد اللغات لاكتشاف الكتب المناسبة للحظة الحالية، بالاعتماد على المزاج والطقس والمكان والوقت والفصل واللغة وطول الكتاب.

## فكرة المشروع

بدلاً من البحث عن كتاب باستخدام التصنيف فقط، يساعد ReMood القارئ على اتخاذ قرار سريع ومخصص بناءً على حالته الحالية والجو المحيط به.

## أبرز المزايا

- توصيات مبنية على المزاج، نوع الكتاب، الطقس، المدينة والوقت المحلي.
- دعم العربية والإنجليزية والفرنسية.
- أربعة أنماط لاكتشاف الكتب:
  - كتاب ترند
  - كتاب منسي
  - كتاب قديم
  - كتاب غير معروف
- مساعد لاكتشاف الكتب بالطلبات الطبيعية والبحث الصوتي.
- تقييم كل اقتراح بـ «أعجبني» أو «لم يعجبني» لتحسين البحث التالي.
- حفظ تقييمات القارئ محلياً مع استبعاد الكتب التي لم تعجبه وترجيح الكتب المشابهة لما أعجبه.
- ربط Goodreads لاستبعاد الكتب المقروءة.
- روابط للعثور على الكتاب عبر Open Library وصفحات عربية وهنداوي وجرير وغيرها.
- سجل توصيات وإحصاءات للمستخدمين المسجلين.
- تجربة ضيف لا تتطلب إنشاء حساب للتجربة الأولى.

## تجربة المستخدم

1. يحدد القارئ المدينة والمزاج ونوع الكتاب.
2. يختار اللغة وطول الكتاب والفئة العمرية والمناسبة عند الحاجة.
3. يطلب التوصيات المناسبة للحظة الحالية.
4. يقرأ أسباب ترشيح كل كتاب ومعلوماته وروابط الوصول إليه.
5. يقيّم الاقتراح ليصبح البحث القادم أكثر ملاءمة.

## التقنيات

- React 19 وVite
- TypeScript
- Tailwind CSS وFramer Motion
- Express 5
- PostgreSQL وDrizzle ORM
- Zod وOpenAPI وOrval
- TanStack React Query

## بنية المشروع

- `artifacts/book-weather` — تطبيق الواجهة وتجربة القارئ.
- `artifacts/api-server` — خادم التوصيات والطقس وGoodreads والمساعد.
- `lib/api-spec/openapi.yaml` — عقد API المصدر.
- `lib/api-client-react` — خطافات React المولدة من OpenAPI.
- `lib/api-zod` — مخططات التحقق المولدة.
- `lib/db` — مخططات قاعدة البيانات.

## نقاط القوة التقنية

- لا يعتمد التطبيق على مزود AI كي يعمل؛ توجد مكتبة كتب محلية كخطة بديلة.
- توصيات الضيوف لا تُحفظ في السجل.
- بيانات السجل والإحصاءات معزولة حسب المستخدم.
- حدود للطلبات ومهلة زمنية ومحاولات إعادة للمساعد.
- التحقق من المدخلات وحدود لحجم قوائم Goodreads والتقييمات.
- لا تُرسل عناوين الكتب أو طلبات القراءة إلى نظام التحليلات.

## الحالة الحالية

التطبيق يعمل في بيئة Replit وتم التحقق من بناء الواجهة والخادم وفحص الأنواع. ربط GitHub لم يُفعّل بعد، لذلك يمكن إضافة رابط المستودع هنا بعد الموافقة على تكامل GitHub.

## التشغيل محلياً

```bash
pnpm install
pnpm --filter @workspace/api-server run dev
pnpm --filter @workspace/book-weather run dev
```

## رابط المشروع

- التطبيق: يُضاف رابط النشر بعد النشر.
- GitHub: يُضاف رابط المستودع بعد ربط GitHub.